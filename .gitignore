node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# Live catalog snapshot written by `npm run fetch:cards` — a point-in-time
# export of public.cards, not source data.
scripts/live-cards.json

# Raw per-run balance-sim JSON dumps (large, regeneratable via `npm run
# sim:v4`). Findings get written up in docs/BALANCE_SIM_FINDINGS_*.md; only
# the latest run or two are ever needed, and they stay on disk locally after
# a run rather than living in git.
docs/sim-runs/

# Claude Code internal state (agent worktrees, local session data).
.claude/
